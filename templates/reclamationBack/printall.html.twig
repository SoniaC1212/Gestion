<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <title>Reclamation Print</title>
    <link href="{{ asset('admin/css/sb-admin-2.css') }}" rel="stylesheet" />
    <link rel="stylesheet" href="{{ asset('front/css/style.css') }}" />
</head>
<body>
<header class="clearfix">
    <div id="logo">
        Edu4U
    </div>
    <h1 align="center">Edu4U - toutes les Reclamations </h1>
    <hr></hr>
    <div class="table-wrapper">
      <h1>Reclamations</h1>
    
      <table class="table">
        <thead>
          <tr>
            <th>CIN</th>
            <th>Username</th>
            <th>Email</th>
            <th>Date</th>
            <th>Type</th>
            <th>Description</th>
          </tr>
        </thead>
        <tbody>
          {% for reclamation in reclamations %}
          <tr>
            <td>{{ reclamation.cin }}</td>
            <td>{{ reclamation.username}}</td>
            <td>{{ reclamation.email }}</td>
            <td>{{ reclamation.date ? reclamation.date|date('Y-m-d') : '' }}</td>
            <td>{{ reclamation.type }}</td>
            <td>{{ reclamation.description }}</td>
          </tr>
          {% else %}
          <tr>
            <td colspan="6">no reclamations found</td>
          </tr>
          {% endfor %}
        </tbody>
      </table>
    </div>
</header>
</main>

</body>